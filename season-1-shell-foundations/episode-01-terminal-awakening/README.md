# Episode 01: Terminal Awakening

## 📚 Теория

### Что такое Terminal (Терминал)?

**Terminal** (терминал) — это текстовый интерфейс для взаимодействия с операционной системой. В отличие от графического интерфейса (GUI), где вы кликаете мышкой, в терминале вы **печатаете команды**.

**Почему это важно?**
- ⚡ **Скорость** — команды выполняются быстрее, чем клики мышью
- 🔄 **Автоматизация** — можно писать скрипты для повторяющихся задач
- 🌐 **Удалённый доступ** — можно управлять серверами через SSH
- 💪 **Мощь** — многие вещи МОЖНО сделать только через терминал

> **LILITH:**  
> *"Ты видишь shell. Я вижу тени. Terminal — это не просто инструмент. Это оружие."*

---

### Shell: Ваш интерпретатор команд

**Shell** — это программа, которая принимает ваши команды и передаёт их операционной системе.

**В Ubuntu по умолчанию используется:** **Bash** (Bourne Again Shell)

```bash
max@shadow-server-01:~$
```

Разберём эту строку (prompt):
- `max` — ваше имя пользователя
- `shadow-server-01` — имя компьютера/сервера
- `~` — текущая директория (`~` = домашняя папка)
- `$` — вы обычный пользователь (если `#` — вы root)

> **LILITH:**  
> *"Prompt рассказывает историю. Где ты. Кто ты. Слушай его."*

---

## 🗂️ Файловая система Linux

### Структура: Всё начинается с `/`

В Linux **нет дисков C:, D:, E:** как в Windows. Всё — одно большое дерево, начинающееся с **корня** `/`.

```
/                         (корень — root)
├── home/                 (домашние папки пользователей)
│   ├── max/              (ваша домашняя папка)
│   ├── alex/
│   └── anna/
├── etc/                  (конфигурационные файлы)
├── var/                  (переменные данные: логи, кеш)
│   └── log/              (логи системы)
├── usr/                  (программы и библиотеки)
│   ├── bin/              (исполняемые файлы)
│   └── lib/              (библиотеки)
├── tmp/                  (временные файлы)
├── root/                 (домашняя папка root'а)
└── bin/                  (системные команды)
```

### Основные директории:

| Путь | Что это | Пример |
|------|---------|--------|
| `/` | Корень | Всё начинается здесь |
| `/home/max/` | Ваша домашняя папка | Документы, загрузки, конфиги |
| `/etc/` | Конфигурационные файлы | `/etc/ssh/sshd_config` |
| `/var/log/` | Логи | `/var/log/syslog` |
| `/usr/bin/` | Программы | `/usr/bin/bash` |
| `/tmp/` | Временные файлы | Автоматически удаляются |

> **LILITH:**  
> *"Файловая система как город. `/` — центр. `/home/` — жилые районы. `/var/log/` — архивы. Знай свой город."*

---

## 🧭 Навигация: Как перемещаться

### `pwd` — Где я нахожусь?

**Print Working Directory** — показывает текущую директорию.

```bash
$ pwd
/home/max_test
```

Всегда начинайте с `pwd`, если потерялись.

---

### `ls` — Что здесь находится?

**List** — показывает содержимое директории.

**Базовое использование:**
```bash
$ ls
documents  downloads  scripts  notes.txt
```

**С опциями:**
```bash
$ ls -l          # Детальный список (long format)
$ ls -a          # Все файлы (включая скрытые)
$ ls -lh         # Детальный с размерами (human-readable)
$ ls -la         # Всё вместе
$ ls -lah        # + human-readable размеры
```

**Пример вывода `ls -lh`:**
```bash
$ ls -lh
total 24K
drwxr-xr-x 2 max max 4.0K Oct  2 18:30 documents
drwxr-xr-x 3 max max 4.0K Oct  2 18:31 downloads
-rw-r--r-- 1 max max  156 Oct  2 18:25 notes.txt
-rwxr-xr-x 1 max max  2.1K Oct  2 18:28 script.sh
```

**Разберём строку:**
```
-rw-r--r-- 1 max max  156 Oct  2 18:25 notes.txt
│││││││││  │  │   │    │    │         │
│││││││││  │  │   │    │    │         └─ Имя файла
│││││││││  │  │   │    │    └─────────── Дата изменения
│││││││││  │  │   │    └──────────────── Размер (156 байт)
│││││││││  │  │   └───────────────────── Группа (max)
│││││││││  │  └───────────────────────── Владелец (max)
│││││││││  └──────────────────────────── Количество ссылок
│││││││││
│││││││└┴─ Права для остальных (r-- = только чтение)
││││└┴──── Права для группы (r-- = только чтение)
│└┴┴────── Права для владельца (rw- = чтение + запись)
└───────── Тип: - = файл, d = директория, l = ссылка
```

> **LILITH:**  
> *"`ls -la` — твоя первая команда на любом сервере. Увидишь всё, что скрыто."*

---

### `cd` — Перейти в директорию

**Change Directory** — перемещение между папками.

```bash
$ cd documents           # Перейти в documents/
$ cd /home/max/scripts   # Абсолютный путь
$ cd ..                  # На уровень выше
$ cd ../..               # На два уровня выше
$ cd ~                   # В домашнюю папку
$ cd /                   # В корень системы
$ cd -                   # Вернуться в предыдущую директорию
```

**Абсолютный vs Относительный путь:**

**Абсолютный** — начинается с `/`:
```bash
$ cd /home/max/documents
```

**Относительный** — относительно текущей позиции:
```bash
$ pwd
/home/max
$ cd documents      # = /home/max/documents
$ cd ../downloads   # = /home/max/downloads
```

---

## 📄 Работа с файлами

### `cat` — Вывести содержимое файла

**Concatenate** — показывает содержимое файла целиком.

```bash
$ cat notes.txt
Встреча с Viktor — 14:00
Координаты: 55.7558°N, 37.6173°E
IP: 185.192.45.118
```

**Для больших файлов:**
```bash
$ cat large_file.txt     # Весь файл сразу (может быть много!)
```

---

### `less` — Постраничный просмотр

Для больших файлов лучше использовать `less`:

```bash
$ less large_file.txt
```

**Управление:**
- `Space` — следующая страница
- `b` — предыдущая страница
- `/поиск` — поиск текста
- `q` — выход

> **LILITH:**  
> *"`cat` для маленьких файлов. `less` для больших. Логи не врут, но их много."*

---

### `head` и `tail` — Первые/последние строки

**Первые 10 строк:**
```bash
$ head file.txt
```

**Последние 10 строк:**
```bash
$ tail file.txt
```

**Указать количество:**
```bash
$ head -n 5 file.txt     # Первые 5 строк
$ tail -n 20 file.txt    # Последние 20 строк
```

**Следить за файлом в реальном времени:**
```bash
$ tail -f /var/log/syslog
```
(Ctrl+C для выхода)

---

## 🔍 Поиск файлов

### `find` — Мощный поиск

**Найти все файлы `.txt`:**
```bash
$ find . -name "*.txt"
./documents/report.txt
./notes.txt
./scripts/readme.txt
```

**Найти скрытые файлы:**
```bash
$ find . -name ".*"
./.bashrc
./.profile
./documents/.secret
```

**Найти файлы по имени:**
```bash
$ find . -name "briefing*"
./documents/briefing.txt
./backup/briefing_old.txt
```

**Найти только файлы (не директории):**
```bash
$ find . -type f -name "*.sh"
```

**Найти только директории:**
```bash
$ find . -type d -name "scripts"
```

> **LILITH:**  
> *"`find` — твой radar. Ничто не скроется. Даже в тенях."*

---

## 📁 Создание и удаление

### `mkdir` — Создать директорию

```bash
$ mkdir new_folder
$ mkdir -p path/to/nested/folder    # Создать вложенные папки
```

---

### `touch` — Создать пустой файл

```bash
$ touch new_file.txt
```

---

### `rm` — Удалить файл

```bash
$ rm file.txt              # Удалить файл
$ rm -r folder/            # Удалить директорию рекурсивно
$ rm -rf folder/           # Удалить без подтверждения (опасно!)
```

**⚠️ ОСТОРОЖНО:**
```bash
$ rm -rf /        # НИКОГДА НЕ ДЕЛАЙТЕ ЭТО! Удалит ВСЁ!
```

> **LILITH:**  
> *"`rm -rf` как заряженный пистолет. Одно неверное движение — и всё пропало."*

---

### `cp` — Копировать

```bash
$ cp file.txt file_backup.txt          # Копировать файл
$ cp -r folder/ folder_backup/         # Копировать директорию
```

---

### `mv` — Переместить или переименовать

```bash
$ mv old_name.txt new_name.txt         # Переименовать
$ mv file.txt documents/               # Переместить в папку
```

---

## 🔐 Скрытые файлы

В Linux **скрытые файлы** начинаются с **точки** `.`

**Примеры:**
- `.bashrc` — конфигурация bash
- `.ssh/` — SSH ключи
- `.config/` — конфигурации приложений
- `.secret_location` — ваши секретные данные

**Как увидеть:**
```bash
$ ls -a      # Покажет ВСЕ файлы, включая скрытые
```

> **LILITH:**  
> *"Скрытые файлы не невидимы. Просто большинство не знает, где искать. `ls -a` — твой фонарик."*

---

## 🎯 Man Pages — Документация

**Не знаешь, как работает команда?** Используй `man`:

```bash
$ man ls
$ man find
$ man cat
```

**Или `--help`:**
```bash
$ ls --help
$ find --help
```

> **LILITH:**  
> *"RTFM — Read The Fucking Manual. Man pages — твоя библиотека. Используй."*

---

## ✍️ Создание Bash скрипта

### Что такое скрипт?

**Bash скрипт** — это файл с командами, которые выполняются последовательно.

### Структура:

```bash
#!/bin/bash
# Это комментарий

echo "Привет, мир!"
pwd
ls -la
```

### Shebang `#!/bin/bash`

Первая строка **обязательна**. Она говорит системе: "Используй bash для выполнения этого файла".

### Создание скрипта:

```bash
$ touch my_script.sh
$ chmod +x my_script.sh      # Сделать исполняемым
$ ./my_script.sh             # Запустить
```

**Пример скрипта:**

```bash
#!/bin/bash

echo "Начинаю поиск..."
echo ""

# Найти все .txt файлы
echo "Найденные .txt файлы:"
find . -name "*.txt"

echo ""
echo "Поиск завершён!"
```

**Запуск:**
```bash
$ chmod +x search.sh
$ ./search.sh
```

> **LILITH:**  
> *"Bash скрипт — твоя первая автоматизация. Не делай руками то, что может сделать скрипт."*

---

## 📊 Контрольные вопросы

### Вопрос 1: Навигация
Вы находитесь в `/home/max/documents/reports/`. Как перейти в `/home/max/scripts/` одной командой?

<details>
<summary>💡 Подсказка</summary>

Нужно подняться на два уровня вверх (`..` дважды), потом спуститься в `scripts/`.
</details>

<details>
<summary>✅ Ответ</summary>

```bash
$ cd ../../scripts/
```

**LILITH:**  
*"Относительные пути быстрее. Но если потерялся — `cd /home/max/scripts/` всегда сработает."*
</details>

---

### Вопрос 2: Скрытые файлы
Как найти ВСЕ скрытые файлы в текущей директории и всех подпапках?

<details>
<summary>💡 Подсказка</summary>

`find` + условие на имя начинающееся с `.`
</details>

<details>
<summary>✅ Ответ</summary>

```bash
$ find . -name ".*"
```

Или только файлы (не директории):
```bash
$ find . -type f -name ".*"
```

**LILITH:**  
*"`find` видит всё. Даже то, что хотели скрыть."*
</details>

---

### Вопрос 3: Чтение файлов
У вас есть файл `access.log` на 10,000 строк. Как посмотреть только последние 50 строк?

<details>
<summary>💡 Подсказка</summary>

Команда для последних строк + указание количества.
</details>

<details>
<summary>✅ Ответ</summary>

```bash
$ tail -n 50 access.log
```

Или следить в реальном времени:
```bash
$ tail -f access.log
```

**LILITH:**  
*"Логи не врут. Но их много. `tail` покажет самое свежее."*
</details>

---

### Вопрос 4: Создание скрипта
Почему этот скрипт не запускается?

```bash
$ cat test.sh
echo "Hello"
$ ./test.sh
bash: ./test.sh: Permission denied
```

<details>
<summary>💡 Подсказка</summary>

Проверьте права доступа. Файл исполняемый?
</details>

<details>
<summary>✅ Ответ</summary>

Файл не исполняемый. Нужно:

```bash
$ chmod +x test.sh
$ ./test.sh
Hello
```

Также можно проверить права:
```bash
$ ls -l test.sh
-rw-r--r-- 1 max max 13 Oct  2 20:15 test.sh
         ^
         └─ Нет 'x' (execute)
```

**LILITH:**  
*"`chmod +x` — дай файлу право на жизнь. Без 'x' он мёртв."*
</details>

---

### Вопрос 5: Опасная команда
Что делает эта команда и почему её НИКОГДА нельзя запускать?

```bash
$ rm -rf /
```

<details>
<summary>💡 Подсказка</summary>

`rm` = удалить, `-rf` = рекурсивно и без подтверждения, `/` = корень системы.
</details>

<details>
<summary>✅ Ответ</summary>

Удаляет **ВСЮ** файловую систему, начиная с корня `/`.

- `rm` — удалить
- `-r` — рекурсивно (все папки и подпапки)
- `-f` — force (без подтверждения)
- `/` — начиная с корня

**Результат:** Система полностью уничтожена. Все данные потеряны. Recovery невозможен.

**LILITH:**  
*"`rm -rf /` — цифровое самоубийство. Современные системы защищаются, но лучше даже не пытаться."*
</details>

---

## 🎯 Практическая задача

Создайте скрипт `system_info.sh`, который выводит:
1. Текущую директорию
2. Список всех файлов в текущей директории (включая скрытые)
3. Содержимое файла `.bashrc` (первые 10 строк)

**Шаблон:**

```bash
#!/bin/bash

echo "=== System Information ==="
echo ""

# 1. Текущая директория
echo "Текущая директория:"
# TODO: ваша команда

echo ""

# 2. Список файлов
echo "Файлы в директории:"
# TODO: ваша команда

echo ""

# 3. Содержимое .bashrc (первые 10 строк)
echo "Первые 10 строк .bashrc:"
# TODO: ваша команда
```

<details>
<summary>✅ Решение</summary>

```bash
#!/bin/bash

echo "=== System Information ==="
echo ""

# 1. Текущая директория
echo "Текущая директория:"
pwd

echo ""

# 2. Список файлов
echo "Файлы в директории:"
ls -la

echo ""

# 3. Содержимое .bashrc (первые 10 строк)
echo "Первые 10 строк .bashrc:"
head -n 10 ~/.bashrc
```

**Запуск:**
```bash
$ chmod +x system_info.sh
$ ./system_info.sh
```
</details>

---

## 📚 Дополнительные материалы

### Рекомендуемые команды для изучения:
- `grep` — поиск текста в файлах
- `wc` — подсчёт строк/слов/символов
- `sort` — сортировка строк
- `uniq` — удаление дубликатов
- `echo` — вывод текста

### Полезные ресурсы:
- `man bash` — полная документация Bash
- https://explainshell.com/ — объяснение команд
- https://www.shellcheck.net/ — проверка bash скриптов

---

## 🎬 Следующие шаги

Вы изучили основы терминала. Теперь:
1. Выполните миссию из `mission.md`
2. Найдите все три скрытых файла
3. Создайте автоматический скрипт
4. Переходите к Episode 02 — Shell Scripting Basics

> **LILITH:**  
> *"Терминал — твоя новая реальность. Привыкай. Скоро ты не сможешь без него жить."*

---

<div align="center">

**OPERATION KERNEL SHADOWS**  
*Episode 01 — Terminal Awakening*

*"Ты видишь shell. Я вижу тени."* — LILITH

</div>

