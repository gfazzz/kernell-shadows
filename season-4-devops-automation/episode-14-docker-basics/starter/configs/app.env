# KERNEL SHADOWS: Episode 14 — Docker Basics
# Environment variables for Operation Shadow containers
# Amsterdam, Netherlands (Day 27-28)

# ============================================================================
# Application Configuration
# ============================================================================
# TODO 1: Заполни основные параметры приложения
APP_NAME=operation-shadow
APP_ENV=production  # development, staging, production
APP_VERSION=1.0.0
APP_PORT=8080

# ============================================================================
# Nginx Configuration
# ============================================================================
# TODO 2: Настрой Nginx параметры
NGINX_HOST=localhost
NGINX_PORT=80

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
# TODO 3: Настрой параметры подключения к базе данных
# Подсказка: Используй имя сервиса из docker-compose.yml (db)
DB_HOST=db  # Имя сервиса из docker-compose
DB_PORT=5432
DB_NAME=operation_shadow
DB_USER=shadow_user
# ВАЖНО: НЕ храни пароли в этом файле!
# Используй Docker secrets или отдельный .env файл (не коммитить в git!)

# ============================================================================
# СПРАВКА: Как использовать
#
# 1. С docker-compose:
#    docker-compose --env-file configs/app.env up -d
#
# 2. С docker run:
#    docker run --env-file configs/app.env myimage
#
# 3. В docker-compose.yml:
#    services:
#      web:
#        env_file:
#          - configs/app.env
#
# ============================================================================
# SECURITY WARNING:
#
# ⚠️ НИКОГДА не коммить файлы с паролями в git!
# ⚠️ Добавь *.env в .gitignore
# ⚠️ Используй Docker secrets для production:
#
# secrets:
#   db_password:
#     file: ./secrets/db_password.txt
#
# environment:
#   POSTGRES_PASSWORD_FILE: /run/secrets/db_password
#
# ============================================================================

