# ArduPilot Critical Parameters - Episode 22
# KERNEL SHADOWS Surveillance Drone

## SAFETY PARAMETERS (CRITICAL!)
ARMING_CHECK=1          # All pre-arm safety checks
FENCE_ENABLE=1          # Geofence enabled
FENCE_ACTION=1          # RTL on breach
FENCE_ALT_MAX=100       # Max altitude 100m
FENCE_RADIUS=500        # Max radius 500m
FS_GCS_ENABLE=1         # Failsafe on GCS loss
FS_THR_ENABLE=1         # Failsafe on throttle loss
RTL_ALT=50              # RTL altitude 50m
LAND_SPEED=50           # Landing speed (cm/s)

## FLIGHT MODES
FLTMODE1=0              # Stabilize
FLTMODE2=2              # Alt Hold  
FLTMODE3=3              # Auto (mission)
FLTMODE4=5              # Loiter
FLTMODE5=6              # RTL (Return to Launch)
FLTMODE6=9              # Land

## BATTERY (CRITICAL!)
BATT_CAPACITY=5000      # 5000 mAh
BATT_LOW_VOLT=14.4      # Low voltage warning (4S LiPo)
BATT_CRT_VOLT=13.6      # Critical voltage (RTL)
BATT_FS_LOW_ACT=2       # RTL on low battery
BATT_FS_CRT_ACT=1       # Land on critical

## GPS
GPS_TYPE=1              # Auto detect
GPS_AUTO_SWITCH=1       # Auto switch GPS
EK2_GPS_TYPE=0          # GPS 3D velocity

## TELEMETRY
SR0_EXTRA1=4            # Attitude at 4Hz
SR0_EXTRA2=4            # VFR_HUD at 4Hz
SR0_EXTRA3=2            # AHRS, HWSTATUS at 2Hz
SR0_POSITION=2          # Position at 2Hz
SR0_RAW_SENS=2          # IMU at 2Hz
SR0_RC_CHAN=2           # RC channels at 2Hz

## LOGGING
LOG_BACKEND_TYPE=1      # MAVLink logging
LOG_BITMASK=65535       # Log everything
LOG_DISARMED=0          # Don't log when disarmed

## TUNING (conservative for safety)
PSC_VELXY_P=1.0
PSC_VELXY_I=0.5
PSC_VELZ_P=5.0
ATC_RAT_PIT_P=0.135
ATC_RAT_RLL_P=0.135
ATC_RAT_YAW_P=0.18

## Notes:
# - Test в симуляторе first!
# - Adjust tuning based on drone weight/props
# - Battery voltages для 4S LiPo (adjust if different)
